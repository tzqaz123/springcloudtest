spring:
  cloud:
    config:
      name: provider-user-8000    #加载远程配置中心文件名称
      #profile: prod                #动态加载开发环境配置文件
      label:  master              #获取master默认分支
      uri: http://localhost:9080/